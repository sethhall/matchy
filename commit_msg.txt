Add SHA384 support and fix clippy lints

Hash Extraction:
- Add HashType::Sha384 for 96 hex character hashes
- Update hash length detection (32/40/64/96 → MD5/SHA1/SHA256/SHA384)
- Add sha384_count to WorkerStats for tracking
- Add test_hash_extraction_sha384 test

Clippy Fixes:
- Add HashType::is_empty() method (clippy::len_without_is_empty)
- Fix needless_range_loop in find_word_boundaries
- Add Hash variant handling in all match statements:
  - extractor_demo.rs
  - extract_cmd.rs
  - sequential.rs match processor
- Hash lookups use db.lookup() like Domain/Email patterns

Bug Fix:
- Remove leftover junk code from previous edit (caused cbindgen parse error)

All 201 tests passing.
